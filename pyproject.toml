[project]
name = "google-pollen-exporter"
version = "0.1.0"
description = "Prometheus(OpenMetrics) exporter for Google Pollen API"
readme = "README.md"
requires-python = ">=3.14.3"
dependencies = [
    "dacite>=1.9.2",
    "prometheus-client>=0.24.1",
    "pyyaml>=6.0.3",
    "requests>=2.32.5",
]
